import { PContactProps } from "../../../types/types";
import * as C from "./style";

export default function CompanyAdress(props: PContactProps) {
  return <C.Container>{props.PContent}</C.Container>;
}
