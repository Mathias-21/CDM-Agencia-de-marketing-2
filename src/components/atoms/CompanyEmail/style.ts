import styled from "styled-components";

export const Container = styled.p`
  font-family: "open sans", sans-serif;
  font-size: 0.94rem;
  margin: 0.9rem 0 2.6rem 0;
  color: #ff7801;
  cursor: pointer;
  transition: 0.5s;

  :hover {
    color: #808080;
  }
`;
